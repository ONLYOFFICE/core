#include "HtmlText.h"
namespace HText
{
CHPos CHText::GetNearestPos(unsigned int nPageIndex, double dX, double dY, bool bExcludeSpaces)
{
	return { 0, 0, 0, 0 };
}
CHSelect CHText::SelectWholeWord(unsigned int nPageIndex, double dX, double dY)
{
	return CHSelect();
}
}
